export const squidController = (figure, cursors, keyboard) => {

    // debugger
    
    if (cursors.left.isDown) {
        figure.setVelocityX(-160);

        // figure.anims.play("idle", true);
    }
    else if (cursors.right.isDown) {
        figure.setVelocityX(160);

        // figure.anims.play("idle", true);
    }
    // TODO: Create boundary on top and bottom of road.png
    // if jump then cancel that boundary 
    // else if (cursors.up.isDown){
    //     figure.setVelocityY(-160)
    // }

    else {
        figure.setVelocityX(0);

        figure.anims.play('idle', true);
    }

    if (cursors.space.isDown && figure.body.touching.down) {
        figure.setVelocityY(-230);
    }
}