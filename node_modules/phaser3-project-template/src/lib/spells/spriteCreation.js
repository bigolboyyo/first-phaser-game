export  const  spriteCreation = (x, y, name, physics, platforms, movement) => {
    let sprite = physics.sprite(x, y, name).setCollideWorldBounds(true);
    sprite.name = name
    physics.collider(sprite, platforms)
    movement === null ? null : movement(sprite)
    return sprite
}